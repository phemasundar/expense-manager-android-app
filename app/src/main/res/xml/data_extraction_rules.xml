<?xml version="1.0" encoding="utf-8"?>
<data-extraction-rules>
    <cloud-backup>
        <!-- TODO: Use <include> for shared rules when available -->
        <exclude domain="sharedpref" path="device_protected_prefs.xml"/>
    </cloud-backup>
    <device-transfer>
        <!-- TODO: Use <include> for shared rules when available -->
        <exclude domain="sharedpref" path="device_protected_prefs.xml"/>
    </device-transfer>
</data-extraction-rules>
